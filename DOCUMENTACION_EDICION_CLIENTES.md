# Historia de Usuario: Edición de Clientes

La información detallada en la historia se puede hallar en el anexo correspondiente. Para el desarrollo de esta funcionalidad, se creó la página `EditarCliente.jsx` utilizando React y Tailwind CSS para permitir a administradores y agentes modificar la información de clientes existentes. Los agentes solo pueden editar sus propios clientes, mientras que los administradores pueden editar cualquier cliente. Los clientes inactivos no pueden ser editados hasta que sean reactivados por un administrador.

La interfaz fue realizada utilizando componentes de React con hooks `useState` y `useEffect`, el componente `SelectTipoCliente.jsx` para selector de tipo, y la librería Axios para peticiones HTTP. El componente carga los datos del cliente mediante GET `/api/clientes/:id` al montarse, verifica autenticación y rol antes de cargar, y maneja errores de permisos redirigiendo al panel de clientes. El formulario se prellena con los datos del cliente cargado, incluye validación en tiempo real, muestra el agente asignado en modo solo lectura con mensaje indicando que no se puede cambiar, y deshabilita el botón de guardar si el cliente está inactivo mostrando información de auditoría. El componente incluye protección ante recarga mediante `beforeunload` y modal de confirmación al cancelar si hay cambios sin guardar.

Al enviar el formulario, se valida que el cliente esté activo, se valida completamente el formulario, se preparan los datos (eliminando agenteId ya que no se puede cambiar), y se envía una petición PUT a `/api/clientes/:id` con el token JWT. Si la actualización es exitosa, muestra un toast de éxito y redirige al panel de clientes.

En el backend se utilizó Node.js con Express.js y Prisma como ORM. La función `obtenerCliente` en `cliente.controller.js` busca el cliente por ID, verifica que exista (retorna 404 si no existe), verifica permisos (agente solo puede ver sus propios clientes mediante `cliente.agenteId === usuario.id`), y retorna el cliente con información del agente mediante `include`. La función `actualizarCliente` verifica que el cliente exista, verifica permisos (agente solo puede editar sus propios clientes, admin puede editar cualquier cliente), valida campos obligatorios y formato de email, valida email único excluyendo el cliente actual usando `findFirst` con `id: { not: parseInt(id) }`, y actualiza el cliente sin permitir cambio de agente (el campo `agenteId` no se incluye en los datos actualizados).

Las rutas GET `/api/clientes/:id` (obtener) y PUT `/api/clientes/:id` (actualizar) fueron configuradas en `cliente.routes.js` con middlewares `verificarToken` (autenticación) y `esAgenteOAdmin` (autorización), registradas en `index.js` bajo el prefijo `/api/clientes`. El sistema no permite cambiar el agente asignado, manteniendo la relación fija para preservar la integridad de los datos.
