# Historia de Usuario 4: Registro de Propiedades

La información detallada en la historia se puede hallar en el anexo correspondiente. Para el desarrollo de la historia 4, se creó la página `RegistrarPropiedad.jsx` utilizando React y Tailwind CSS para permitir a administradores y agentes registrar nuevas propiedades inmobiliarias. La interfaz incluye un formulario completo con campos para título, descripción, tipo, estado, transacción, precio, ubicación, características físicas, y gestión de múltiples imágenes (hasta 5).

La interfaz fue realizada utilizando componentes de React con hooks `useState` y `useEffect`, componentes reutilizables `SelectProvincia.jsx` y `SelectTipoPropiedad.jsx` para selectores de enums, y la librería Axios para peticiones HTTP. El componente incluye validación en tiempo real de campos obligatorios y numéricos, gestión de imágenes con vista previa usando `URL.createObjectURL`, validación de tamaño máximo de 5MB por imagen y tipos permitidos (JPEG/JPG/PNG/WEBP), y protección ante recarga de página mediante `beforeunload`.

Al enviar el formulario, la función `handleSubmit` valida todos los campos, crea un `FormData` con todos los datos y archivos, y realiza una petición POST a `/api/propiedades` con `Content-Type: multipart/form-data` mediante Axios. Si es admin, debe seleccionar un agente del selector; si es agente, se auto-asigna. Si el registro es exitoso, muestra un toast de éxito mediante Sonner y redirige al panel de propiedades después de 1 segundo.

En el backend se utilizó Node.js con Express.js y Prisma como ORM. El modelo `Propiedad` fue definido en `schema.prisma` con campos obligatorios: `titulo`, `tipo_propiedad` (enum), `estado_propiedad` (enum), `transaccion` (enum), `precio` (Decimal), `direccion`, `ciudad`, `provincia` (enum), `area_terreno` (Decimal), y `agenteId` (Int). Campos opcionales incluyen `descripcion`, `moneda` (default "USD"), coordenadas, áreas de construcción, características físicas, y `estado_publicacion` (default "disponible"). El modelo `Imagen` fue definido con campos `url` (String) y `propiedadId` (Int) para relación con propiedades.

Se configuró multer en `config/multer.js` para manejo de archivos: almacenamiento en `uploads/` con nombres únicos generados con timestamp y random, límite de 5MB por archivo, y filtro de tipos permitidos mediante regex y verificación de mimetype. La función `crearPropiedad` en `propiedad.controller.js` valida campos obligatorios y opcionales, verifica permisos según rol (admin requiere agenteId, agente se auto-asigna), crea la propiedad con `prisma.propiedad.create` convirtiendo valores a tipos apropiados, y crea registros de imágenes con `prisma.imagen.createMany` usando URLs relativas `/uploads/${file.filename}`.

La ruta POST `/api/propiedades` fue configurada en `propiedad.routes.js` con middlewares `verificarToken` (autenticación), `upload.array('imagenes', 5)` (procesamiento de hasta 5 imágenes), y `crearPropiedad` (controlador), registrada en `index.js` bajo el prefijo `/api/propiedades`.
